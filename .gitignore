# Local TTLock credential probe — contains pasted secrets, never commit.
ttlock-test.ps1

# Local screenshots / scratch
Screenshot*.png

# Brainstorming visual-companion scratch (mockups, server state)
.superpowers/
